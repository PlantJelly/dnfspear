{
    "version": "0.2.0",
    "configurations": [
        {
            "cwd": "${workspaceFolder}/",
            "command": "npm run dev",
            "name": "Launch SvelteKit server",
            "request": "launch",
            "type": "node-terminal"
        }
    ]
}
